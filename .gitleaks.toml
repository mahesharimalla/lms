title = "Enterprise Secret Detection Policy"

[allowlist]
description = "Ignore generated and non-developer files only"

paths = [
  ".git/",
  ".scannerwork/",
  "node_modules/",
  "dist/",
  "build/",
  "target/",
  "coverage/",
  "security-reports/",
  "**/dependency-check*",
  "**/trivy-*"
]
